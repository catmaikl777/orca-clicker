{
  "name": "orca-clicker-api",
  "version": "1.0.0",
  "description": "ORCA Clicker API Server",
  "main": "websocket-server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "admin": "node admin-server.js",
    "start-all": "node start.js",
    "backup": "node scripts/backup-db.js",
    "clear-db": "node scripts/clear-json-db.js --force",
    "clear-postgres": "node scripts/clear-postgres-db.js --force",
    "clear-clans": "node scripts/clear-clans.js",
    "build": "echo 'No build step needed for the API server'"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "dotenv": "^16.6.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "pg": "^8.11.3",
    "ws": "^8.14.2"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "nodemon": "^3.0.2"
  }
}
